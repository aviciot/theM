version: "3.9"

# ============================================================
# Local dev override — removes Traefik dependency.
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d
#
# Differences from production:
#   - proxy-network is created locally (not external)
#   - No Traefik labels active (services still accessible on host ports)
#   - All ports exposed directly on host
# ============================================================

networks:
  proxy-network:
    name: them-proxy-local
    driver: bridge
    external: false

services:
  them-frontend:
    environment:
      NODE_ENV: development
      THE_M_API_URL: "http://them-bridge:8001"
      THE_M_AUTH_URL: "http://them-auth-service:8701"
      NEXT_PUBLIC_BRIDGE_WS_URL: ""
