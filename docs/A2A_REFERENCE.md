# A2A Reference — RETIRED

**Python is permanently OFF. This doc contained Python SDK patterns and is no longer valid.**

Use the Go A2A skill instead — invoke `/a2a` in any Claude Code session.
The skill loads from `.claude/skills/a2a.md` and covers:
- Wire format (JSON-RPC 2.0, part types, methods)
- AgentSpec / SkillSpec / StepSpec contracts
- Interpreter pipeline and step config structs
- Security invariants
- Phase D SDK adoption checklist
- Anti-patterns (including all the Python patterns that were here)

For the full canvas agent architecture: `docs/CANVAS_A2A_AGENT_GENERATION.md`
